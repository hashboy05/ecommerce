# ---------------------------------------------------------------------------
# Pinned dependencies for the E-Commerce REST API.
# Versions match the tested development virtual environment so the Docker
# build is reproducible.
# ---------------------------------------------------------------------------

# Core framework
Flask==3.1.3
flask-smorest==0.47.0
Flask-SQLAlchemy==3.1.1
Flask-JWT-Extended==4.7.4

# ORM / database driver helper
SQLAlchemy==2.0.50
greenlet==3.5.1

# Validation & serialization
marshmallow==4.3.0
webargs==8.7.1
apispec==6.10.0

# JWT
PyJWT==2.13.0

# Flask transitive dependencies (pinned for reproducibility)
Werkzeug==3.1.8
Jinja2==3.1.6
MarkupSafe==3.0.3
itsdangerous==2.2.0
click==8.4.1
blinker==1.9.0
packaging==26.2
typing_extensions==4.15.0

# Production WSGI server (used inside the container)
gunicorn==23.0.0
